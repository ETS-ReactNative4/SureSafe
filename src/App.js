import React from 'react';
import {Text, View} from 'react-native';
import {Colors, Fonts} from './styles';

export default App = () => {
  return (
    <View style={{justifyContent: 'center', alignItems: 'center', flex: 1}}>
      <Text style={Fonts.H1}>Trace Vigan</Text>
    </View>
  );
};
